
<div ng-controller="dashboard">
<div class="col-md-12 ser-grid">


  
  
    <h4>Select the Stats you want to view</h4>
    <div>
    <select name="singleSelect" id="singleSelect" ng-model="selection"  ng-init="selected='Salinity'" ng-change="selectedItemChanged()">
    <option ng-repeat="name in sensorNames"
            value="{{name}}">
      {{name}}
    </option>
	</select>  
    </div>
    <div>
       <highchart id="chart1" config="highchartsNG"></highchart>
    </div>
</div>
